<h2><a href=https://www.codewars.com/kata/5a3f2925b6cfd78fb0000040/train/java target="_blank">Simple reversed parenthesis</a></h2><h3>6 kyu</h3><p>Given a string, return the minimal number of parenthesis reversals needed to make balanced parenthesis. </p><p>For example:</p><pre><code>solve(")(") = 2 Because we need to reverse ")" to "(" and "(" to ")". These are 2 reversals. solve("(((())") = 1 We need to reverse just one "(" parenthesis to make it balanced.solve("(((") = -1 Not possible to form balanced parenthesis. Return -1.</code></pre><p>Parenthesis will be either <code>"("</code> or <code>")"</code>. </p><p>More examples in the test cases. </p><p>Good luck. </p>